Continuing the GalaPo project. Create tests for the super admin dashboard.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/admin/layout/AdminSidebar.test.tsx:
   - Renders all navigation groups and items
   - Pending count badges show correct numbers
   - Active route highlighted with accent color
   - Collapsible sections expand/collapse on click
   - Sidebar collapses to icons-only on toggle
   - "View Public Site" opens in new tab
   - Logout button calls logout function
   - Mobile: sidebar hidden by default

2. /tests/unit/components/admin/dashboard/AdminStatsCard.test.tsx:
   - Renders label, value, and icon
   - Positive change shows green arrow
   - Negative change shows red arrow
   - Clickable cards navigate to correct page
   - Orange highlight when count > 0 (pending items)

3. /tests/unit/components/admin/dashboard/ActivityFeed.test.tsx:
   - Renders activity items in chronological order
   - Each item shows correct icon for type
   - Timestamps in relative format
   - Action links navigate correctly
   - "View All" link works
   - Empty state when no activity

4. /tests/unit/components/admin/dashboard/PendingItems.test.tsx:
   - Tab switching works (Listings, Payments, Claims)
   - Each tab shows correct pending items
   - "Review" / "Verify" buttons navigate to detail pages
   - "View All" links navigate correctly
   - Empty state per tab when nothing pending

5. /tests/unit/components/admin/dashboard/RevenueOverview.test.tsx:
   - Revenue cards show correct amounts with ₱ format
   - Revenue breakdown shows all categories
   - Chart renders (or graceful fallback)
   - Period labels correct (this month, last month, all time)

6. /tests/unit/components/admin/shared/DataTable.test.tsx:
   - Renders column headers
   - Renders data rows correctly
   - Sort: clicking header sorts ascending, click again descending
   - Search: typing filters rows
   - Pagination: correct page count, navigation works
   - Row selection: checkbox selects/deselects
   - Bulk actions: dropdown appears when rows selected
   - Items per page: changing updates pagination
   - Export CSV: triggers download
   - Empty state when no data
   - Loading skeleton while loading
   - Mobile: horizontal scroll works

7. /tests/unit/components/admin/auth/AdminAuthGuard.test.tsx:
   - Admin user → renders children
   - Business owner user → redirects to /admin/login
   - No session → redirects to /admin/login
   - Loading → shows spinner

8. /tests/integration/api/admin-dashboard.test.ts:
   - All admin endpoints return 403 for non-admin users
   - All admin endpoints return 401 for unauthenticated requests
   - GET /api/admin/dashboard/stats returns correct shape and values
   - GET /api/admin/dashboard/activity returns recent activities
   - GET /api/admin/dashboard/top-listings returns top 10
   - GET /api/admin/dashboard/pending returns all pending categories
   - GET /api/admin/users returns paginated business owners
   - GET /api/admin/users/[id] returns user with listings
   - PUT /api/admin/users/[id] toggles is_active
   - GET /api/admin/claims returns claim requests
   - PUT /api/admin/claims/[id] approve → updates listing owner and status
   - PUT /api/admin/claims/[id] reject → clears claim, creates notification
   - GET /api/admin/annual-checks returns checks
   - POST /api/admin/annual-checks/trigger creates check
   - PUT /api/admin/annual-checks/[id] confirm → updates last_verified_at
   - PUT /api/admin/annual-checks/[id] deactivate → sets is_active false + creates reactivation fee

Output all test files with complete code.