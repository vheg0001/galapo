Continuing the GalaPo city directory project (Olongapo City, Philippines).

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI, Supabase Auth
EXISTING: Public layout, homepage, categories, search, listing detail all built.

Build the complete business owner authentication system.

REQUIREMENTS:

1. REGISTRATION PAGE — /app/(public)/register/page.tsx:
   
   LAYOUT:
   - Split layout: Left side (60%) = registration form, Right side (40%) = marketing content
   - Right side content (desktop only, hidden on mobile):
     - "Why List Your Business on GalaPo?"
     - Benefits list with icons:
       - ✅ "Free basic listing forever"
       - ✅ "Reach thousands of Olongapo residents and tourists"
       - ✅ "Manage your business info, photos, and hours"
       - ✅ "Post deals and events"
       - ✅ "Get found on Google with SEO-optimized listing"
       - 🌟 "Upgrade to Featured or Premium for more visibility"
     - Testimonial placeholder: "Join 500+ businesses already on GalaPo"
   - Mobile: form only, benefits shown below form
   
   REGISTRATION FORM:
   - Step 1 — Account Info:
     - Full Name (required)
     - Email (required, validated)
     - Phone Number (required, PH format validation: 09XX-XXX-XXXX or +63)
     - Password (required, min 8 chars, show strength indicator)
     - Confirm Password (required, must match)
     - "I agree to Terms & Conditions and Privacy Policy" checkbox (required, links open in new tab)
     - "Create Account" button (coral orange)
     - "Already have an account? Login" link below
   
   - Step 2 — Business Info (after account created, redirected here):
     - This is actually part of the listing creation flow (Module 9)
     - After registration, redirect to /business/listings/new
     - Show a welcome message: "Account created! Let's set up your business listing."
   
   FORM VALIDATION:
   - Real-time validation on each field (on blur)
   - Email format check
   - Phone format check (Philippine numbers)
   - Password strength: weak (red), medium (yellow), strong (green)
   - Show/hide password toggle (eye icon)
   - Confirm password match check
   - Submit button disabled until all validations pass
   - Show loading spinner on submit
   - Error handling: display Supabase auth errors (email taken, etc.) in a toast notification

2. LOGIN PAGE — /app/(public)/login/page.tsx:
   
   LAYOUT:
   - Centered card (max-width 480px)
   - Logo at top
   - "Welcome Back" heading
   
   LOGIN FORM:
   - Email (required)
   - Password (required)
   - Show/hide password toggle
   - "Forgot Password?" link (below password field)
   - "Login" button (coral orange, full width)
   - "Don't have an account? Register your business" link
   - Loading state on submit
   - Error handling: "Invalid email or password" toast
   - On success: redirect to /business/dashboard

3. FORGOT PASSWORD PAGE — /app/(public)/forgot-password/page.tsx:
   
   - Centered card layout
   - "Reset Your Password" heading
   - Email input field
   - "Send Reset Link" button
   - Success state: "Check your email for reset instructions" message
   - Error state: "Email not found" or "Too many attempts"
   - "Back to Login" link

4. RESET PASSWORD PAGE — /app/(public)/reset-password/page.tsx:
   
   - Centered card layout
   - "Create New Password" heading
   - New password input (with strength indicator)
   - Confirm new password input
   - "Reset Password" button
   - Success: redirect to login with success toast
   - Handles the token from Supabase auth email link

5. BUSINESS OWNER LAYOUT — /app/(business)/layout.tsx:
   
   This is the authenticated layout for business owners (separate from public).
   
   SIDEBAR (desktop) / Bottom nav (mobile):
   - Logo "GalaPo" (links to public homepage)
   - Navigation:
     - 📊 Dashboard (overview)
     - 🏪 My Listings (list of their businesses)
     - ➕ Add New Listing
     - 🏷️ Deals & Offers
     - 📅 Events
     - 💳 Subscription & Billing
     - 🔔 Notifications (with unread count badge)
     - ⚙️ Account Settings
   - Bottom of sidebar:
     - "View Public Site" link (opens in new tab)
     - Logout button
   
   TOP BAR:
   - "GalaPo Business Dashboard" text
   - Right side: notification bell (with unread count), profile avatar/name dropdown
   - Profile dropdown: Account Settings, Logout
   
   MOBILE:
   - Top bar with hamburger menu and notification bell
   - Slide-out sidebar OR bottom tab navigation (Dashboard, Listings, Add, Notifications, More)
   
   DESIGN:
   - Sidebar: Navy blue background (#1B2A4A), white text, coral orange active item
   - Content area: light gray background (#F5F7FA)
   - Professional dashboard aesthetic

6. AUTH GUARD:
   - Create /components/auth/AuthGuard.tsx:
     - Wraps business dashboard pages
     - Checks Supabase auth session
     - If not authenticated → redirect to /login
     - If authenticated but role is not 'business_owner' → redirect to /login with error
     - Shows loading spinner while checking auth
   
   - Create /components/auth/AuthProvider.tsx:
     - Context provider for auth state
     - Provides: user, profile, session, isLoading, isAuthenticated
     - Listens to Supabase auth state changes
     - Auto-refreshes session

7. CREATE COMPONENTS:
   - /components/auth/RegisterForm.tsx
   - /components/auth/LoginForm.tsx
   - /components/auth/ForgotPasswordForm.tsx
   - /components/auth/ResetPasswordForm.tsx
   - /components/auth/PasswordStrengthIndicator.tsx
   - /components/auth/AuthGuard.tsx
   - /components/auth/AuthProvider.tsx
   - /components/business/Sidebar.tsx
   - /components/business/TopBar.tsx
   - /components/business/MobileNav.tsx
   - /components/business/NotificationBell.tsx

8. CREATE ZUSTAND STORE:
   - /store/authStore.ts:
     - user: User | null
     - profile: Profile | null
     - session: Session | null
     - isLoading: boolean
     - isAuthenticated: boolean
     - setUser(), setProfile(), setSession()
     - login(email, password)
     - register(email, password, fullName, phone)
     - logout()
     - resetPassword(email)
     - updatePassword(newPassword)

9. SEO:
   - /register → title: "Register Your Business | GalaPo - Olongapo City Directory"
   - /login → title: "Business Owner Login | GalaPo"
   - /forgot-password → title: "Reset Password | GalaPo"
   - All auth pages: noindex (don't index login/register pages)

Output ALL files with complete code.