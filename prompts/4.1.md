Continuing the GalaPo city directory project (Olongapo City, Philippines).

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI
EXISTING: Project setup, Supabase connected, public layout with header/footer, homepage built.

Build the complete category browsing system.

REQUIREMENTS:

1. ALL CATEGORIES PAGE — /app/(public)/olongapo/categories/page.tsx:
   - Breadcrumb: Home > Categories
   - Page heading: "Browse All Categories in Olongapo"
   - Grid layout (3 columns desktop, 2 tablet, 1 mobile)
   - Each parent category card:
     - Icon (use Lucide React icons matching category, e.g., Utensils for Food & Dining, ShoppingBag for Shopping, Heart for Health, etc.)
     - Category name
     - Total listing count (approved + active)
     - List of subcategory names (max 5 shown, "+X more" if overflow)
     - Hover: lift + shadow + accent border left
     - Click → goes to /olongapo/[category-slug]
   - SEO: generateMetadata() with title "Browse Business Categories in Olongapo City | GalaPo"
   - Ad slot: top of page (category_banner placement)

2. CATEGORY DETAIL PAGE — /app/(public)/olongapo/[category]/page.tsx:
   - Dynamic route catching parent category slug
   - Breadcrumb: Home > Categories > [Category Name]
   - Page heading: "[Category Name] in Olongapo City"
   - Subheading: "[X] businesses found"
   
   LEFT SIDEBAR (desktop only, collapsible on mobile as filter drawer):
   - Subcategory filter:
     - List all subcategories under this parent
     - Each with listing count
     - Clicking filters the listing grid
     - "All [Category]" option to reset
   - Barangay filter:
     - Grouped: "Olongapo City" header, "SBFZ" header, "Nearby Areas" header
     - Checkbox multi-select
   - Price range filter (if applicable to category)
   - "Open Now" toggle (compares operating_hours with current time in PH timezone)
   - "Featured Only" toggle
   - Clear all filters button
   - Mobile: "Filters" button opens slide-up drawer with all filters
   
   MAIN CONTENT AREA:
   - View toggle: Grid view (default) | List view | Map view
   - Sort dropdown: "Featured First" (default) | "Newest" | "Name A-Z" | "Name Z-A"
   - Listing count: "Showing X of Y results"
   
   GRID VIEW:
   - 3 columns desktop, 2 tablet, 1 mobile
   - Use the ListingCard component from Module 3
   - Top search placement cards (paid positions 1-3) appear FIRST with "Sponsored" badge
   - Then featured listings with "Featured" badge
   - Then regular listings
   - Pagination at bottom (20 per page)
   
   LIST VIEW:
   - Full-width horizontal cards
   - Shows more info: business name, address, phone, short description, operating hours, category tags
   - Same ordering: sponsored > featured > regular
   
   MAP VIEW:
   - Full-width Leaflet map (height: 500px)
   - All filtered listings shown as pins
   - Click pin → popup with: business name, category, address, phone, "View Details" link
   - List of results below the map (scrollable)
   - Map updates when filters change
   
   AD PLACEMENTS:
   - Sidebar ad (desktop): 300x250, <AdSlot location="search_sidebar" />
   - Inline ad: after every 5th listing in grid/list view, <AdSlot location="search_inline" />
   
   SEO:
   - generateMetadata() with:
     - title: "Best [Category Name] in Olongapo City | GalaPo"
     - description: "Find the best [category] in Olongapo City. Browse [X] verified businesses with addresses, phone numbers, and more."
   - Schema.org ItemList structured data for the listings

3. SUBCATEGORY PAGE — /app/(public)/olongapo/[category]/[subcategory]/page.tsx:
   - Same layout as category page but filtered to subcategory
   - Breadcrumb: Home > Categories > [Parent Category] > [Subcategory]
   - Heading: "[Subcategory Name] in Olongapo City"
   - Same filters (minus subcategory filter since already filtered)
   - SEO: "Best [Subcategory] in Olongapo City | GalaPo"

4. CREATE COMPONENTS:
   - /components/public/CategoryCard.tsx — card for the all-categories grid
   - /components/public/CategorySidebar.tsx — filter sidebar
   - /components/public/FilterDrawer.tsx — mobile filter drawer
   - /components/public/ViewToggle.tsx — grid/list/map toggle buttons
   - /components/public/SortDropdown.tsx — sort options dropdown
   - /components/public/ListingGrid.tsx — grid view wrapper with pagination
   - /components/public/ListingList.tsx — list view wrapper
   - /components/public/ListingMapView.tsx — map view with pins + results list
   - /components/public/SponsoredBadge.tsx — "Sponsored" indicator badge
   - /components/public/ResultsCount.tsx — "Showing X of Y results"
   - /components/public/ActiveFilters.tsx — shows active filter chips with remove button

5. URL STRUCTURE:
   - /olongapo/categories → all categories
   - /olongapo/food-dining → Food & Dining category
   - /olongapo/food-dining/restaurants → Restaurants subcategory
   - /olongapo/food-dining?barangay=barretto → filtered by barangay
   - /olongapo/food-dining?barangay=barretto&sort=newest → filtered + sorted
   - All filters are URL query params (shareable, SEO-friendly)

6. PAGINATION:
   - Use URL-based pagination: ?page=1, ?page=2
   - Show: Previous, page numbers, Next
   - Preserve all filters when paginating
   - Scroll to top on page change

7. EMPTY STATES:
   - No listings found: show illustration + "No businesses found in this category" + suggestion to adjust filters
   - No subcategory listings: show parent category link

8. LOADING STATES:
   - Skeleton loaders for listing cards while data loads
   - Map shows loading spinner while pins load

Output ALL files with complete code. Ensure map components use dynamic import with { ssr: false }.