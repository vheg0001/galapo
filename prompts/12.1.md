Continuing the GalaPo city directory project.

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI
EXISTING: Admin layout with DataTable, all admin listing management built.

Build admin management pages for categories, subcategories, dynamic fields, and barangays.

REQUIREMENTS:

1. CATEGORIES PAGE — /app/(admin)/admin/categories/page.tsx:
   
   PAGE HEADER: "Category Management" + "Add Category" button
   
   LAYOUT: Two-panel (like file explorer):
   
   LEFT PANEL (40%): Category Tree
   - Expandable tree view of all categories
   - Each parent category: icon + name + listing count + expand arrow
   - Click expand → shows subcategories indented
   - Each subcategory: name + listing count
   - Click any category/subcategory → loads detail in right panel
   - Drag-and-drop to reorder (sort_order)
   - Right-click context menu: Edit, Add Subcategory, Toggle Active, Delete
   - Active categories: normal text. Inactive: grayed out + strikethrough
   - Search input above tree to filter categories
   
   RIGHT PANEL (60%): Category Detail / Edit
   
   When a category is selected:
   
   SECTION: Category Info (editable inline)
   - Name (text input)
   - Slug (auto-generated, editable)
   - Description (textarea)
   - Icon (icon picker — select from Lucide icons list with search/preview)
   - Parent (dropdown — NULL for parent categories, select parent for subcategories)
   - Sort Order (number)
   - Is Active (toggle)
   - Listing Count (read-only)
   - "Save Changes" button
   - "Delete Category" button (only if no listings use it, otherwise show warning)
   
   SECTION: Dynamic Fields (for this category)
   - Table/list of all category_fields belonging to this category
   - Columns: Sort Order (drag handle), Field Label, Field Type, Required, Subcategory-Specific, Active, Actions
   - "Add Field" button → opens field editor modal
   - Click field → opens field editor modal for editing
   - Drag to reorder fields
   - Toggle required, toggle active inline
   
   FIELD EDITOR MODAL:
   - Field Label (required, e.g., "Cuisine Type")
   - Field Name (auto-generated from label in snake_case, editable)
   - Field Type dropdown: text, textarea, number, currency, boolean, select, multi_select, url, phone, email, image_gallery, menu_items, time_range, json
   - Is Required toggle
   - Placeholder text (for text/number inputs)
   - Help Text (shown below field in forms)
   - Subcategory (dropdown: "All subcategories" or specific subcategory)
   - Options (only shown for select/multi_select):
     - Dynamic list builder: type option → Enter → added to list
     - Reorder options by drag
     - Delete option with X
   - Validation Rules (only shown for number/currency):
     - Min value
     - Max value
   - Sort Order (number)
   - Is Active toggle
   - Preview: live preview of how the field will look in the listing form
   - "Save Field" / "Update Field" button
   - "Delete Field" button (with confirmation)

2. ADD CATEGORY MODAL:
   - Name (required)
   - Slug (auto-generated)
   - Description
   - Icon picker
   - Parent Category (dropdown, "None — this is a parent category" option)
   - Sort Order
   - "Create Category" button

3. BARANGAYS PAGE — /app/(admin)/admin/barangays/page.tsx:
   
   PAGE HEADER: "Barangays & Areas Management" + "Add Area" button
   
   DATA TABLE:
   - Columns: Sort Order (drag), Name, Slug, Group, Listing Count, Active, Actions
   - Group column: auto-detect based on sort_order ranges:
     - 1-99: "Olongapo City"
     - 100-199: "SBFZ"
     - 200-299: "Subic"
     - 300-399: "Castillejos"
     - 400-499: "San Marcelino"
     - 500+: "Other"
   - Active toggle inline (switch)
   - Actions: Edit, Delete (only if no listings reference it)
   - Drag rows to reorder (updates sort_order)
   - Search input
   
   ADD/EDIT BARANGAY MODAL:
   - Name (required)
   - Slug (auto-generated)
   - Sort Order (number — determines grouping)
   - Is Active toggle
   - Hint text explaining sort_order grouping logic
   - "Save" button

4. STATIC PAGES MANAGEMENT — /app/(admin)/admin/pages/page.tsx:
   
   PAGE HEADER: "Static Pages" + "Add Page" button
   
   DATA TABLE:
   - Columns: Title, Slug, Published, Last Updated, Actions
   - Actions: Edit, Toggle Published, Delete
   
   PAGE EDITOR — /app/(admin)/admin/pages/[id]/edit/page.tsx:
   - Title (text input)
   - Slug (auto-generated, editable)
   - Content (rich text editor — use a proper WYSIWYG like TipTap or React-Quill compatible with Next.js 16):
     - Bold, italic, underline, strikethrough
     - Headings (H1-H4)
     - Bullet list, numbered list
     - Links
     - Images (upload or paste URL)
     - Block quotes
     - Code blocks
     - Tables
     - Horizontal rule
   - SEO section:
     - Meta Title (max 70 chars with counter)
     - Meta Description (max 160 chars with counter)
   - Is Published toggle
   - "Save Page" button
   - "Preview" button (opens public preview in new tab)

5. SITE SETTINGS PAGE — /app/(admin)/admin/settings/page.tsx:
   
   TAB LAYOUT:
   
   TAB: General
   - Site Name
   - Site Tagline
   - Contact Email
   - Contact Phone
   - Contact Address
   - Social Links: Facebook URL, Instagram URL, TikTok URL
   - "Save" button
   
   TAB: Pricing
   - Featured Listing Monthly Price (₱)
   - Premium Listing Monthly Price (₱)
   - Top Search Monthly Price (₱)
   - Ad Placement Monthly Price (₱)
   - Reactivation Fee Amount (₱)
   - Subscription Expiry Warning Days (number)
   - Annual Check Response Days (number)
   - "Save" button
   
   TAB: Payment Info
   - GCash Number
   - GCash Account Name
   - Bank Name
   - Bank Account Number
   - Bank Account Name
   - Payment Instructions (textarea — shown to business owners during payment)
   - "Save" button
   
   TAB: SEO
   - Default Meta Title Template (e.g., "[Page Title] | GalaPo")
   - Default Meta Description
   - Google Analytics Tracking ID
   - Google Search Console Verification Code
   - "Save" button
   
   TAB: Ads
   - Google AdSense Publisher ID
   - Default AdSense Slot IDs per placement location
   - Enable/Disable AdSense toggle
   - "Save" button

6. CREATE COMPONENTS:
   - /components/admin/categories/CategoryTree.tsx — expandable tree view
   - /components/admin/categories/CategoryDetail.tsx — right panel detail/edit
   - /components/admin/categories/DynamicFieldsList.tsx — fields table for a category
   - /components/admin/categories/FieldEditorModal.tsx — add/edit dynamic field
   - /components/admin/categories/IconPicker.tsx — Lucide icon selector
   - /components/admin/categories/OptionsBuilder.tsx — select/multi_select options editor
   - /components/admin/categories/AddCategoryModal.tsx
   - /components/admin/barangays/BarangayTable.tsx
   - /components/admin/barangays/BarangayModal.tsx — add/edit barangay
   - /components/admin/pages/PageEditor.tsx — rich text editor wrapper
   - /components/admin/pages/RichTextEditor.tsx — WYSIWYG editor component
   - /components/admin/settings/GeneralSettings.tsx
   - /components/admin/settings/PricingSettings.tsx
   - /components/admin/settings/PaymentSettings.tsx
   - /components/admin/settings/SEOSettings.tsx
   - /components/admin/settings/AdsSettings.tsx

7. Rich Text Editor:
   - Use TipTap (latest 2026 version) — best Next.js 16 compatibility
   - Install: @tiptap/react, @tiptap/starter-kit, @tiptap/extension-*
   - Configure all formatting options listed above
   - Image upload integrates with Supabase Storage
   - Output: HTML string stored in database

Output ALL files with complete code. The category tree with drag-reorder is critical for UX.