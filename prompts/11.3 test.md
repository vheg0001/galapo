Continuing the GalaPo project. Create tests for admin listing management.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/admin/listings/ListingsFilterBar.test.tsx:
   - Status tabs render with counts
   - Clicking tab filters listings
   - Category dropdown filters
   - Search input filters by business name
   - "Clear Filters" resets all
   - "More Filters" expands additional filters
   - Active filters shown as chips

2. /tests/unit/components/admin/listings/ListingDetailView.test.tsx:
   - Renders all listing information sections
   - Status badge shows correct color
   - Approve button visible for pending listings
   - Reject button visible for pending listings
   - Approve/reject hidden for already approved listings
   - Toggle switches work for active/featured
   - Owner info section shows correctly (owner vs pre-populated)
   - Claim section shows for claimed_pending with proof viewer
   - Analytics summary displays stats
   - Admin notes section allows adding notes

3. /tests/unit/components/admin/listings/RejectionModal.test.tsx:
   - Modal opens on trigger
   - Reason textarea required
   - Template dropdown pre-fills reason
   - Custom reason editable
   - "Reject" button calls API
   - "Cancel" closes modal
   - Loading state during submission

4. /tests/unit/components/admin/listings/BulkActionsBar.test.tsx:
   - Appears when rows selected
   - Shows selected count
   - Dropdown with bulk actions
   - Confirm dialog for destructive actions
   - "Deselect All" clears selection

5. /tests/unit/components/admin/claims/ClaimDetailView.test.tsx:
   - Shows listing info, claimant info
   - Proof document viewer renders (image inline, PDF download link)
   - Approve button → confirmation dialog
   - Reject button → reason modal
   - After approval: listing owner updated

6. /tests/integration/api/admin-listings.test.ts:
   - Non-admin → 403 on all endpoints
   - GET /api/admin/listings returns all listings with filters
   - GET with status=pending returns only pending
   - GET with search=keyword filters correctly
   - GET with pagination works
   - GET /api/admin/listings/counts returns correct counts
   - POST /api/admin/listings creates listing with admin extras
   - GET /api/admin/listings/[id] returns full detail
   - PUT /api/admin/listings/[id] updates without re-approval
   - POST /api/admin/listings/[id]/approve changes status + creates notification
   - POST /api/admin/listings/[id]/reject changes status + stores reason + creates notification
   - POST /api/admin/listings/bulk approve multiple → all approved
   - POST /api/admin/listings/bulk reject multiple → all rejected with reason
   - POST /api/admin/listings/[id]/notes creates note
   - GET /api/admin/listings/[id]/notes returns notes
   - POST /api/admin/listings/[id]/assign-owner updates owner
   - GET /api/admin/listings/[id]/analytics returns stats
   - PUT /api/admin/claims/[id] approve → updates listing owner + notifications
   - PUT /api/admin/claims/[id] reject → clears claim + notification with reason

Output all test files with complete code.