Continuing the GalaPo city directory project (Olongapo City, Philippines).

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI
EXISTING: Public site, business owner auth/dashboard, listing CRUD all built.

Build the complete super admin authentication and dashboard.

REQUIREMENTS:

1. ADMIN LOGIN PAGE — /app/(public)/admin/login/page.tsx:
   - Separate login page from business owner login
   - Centered card with dark theme (navy background)
   - "GalaPo Admin" heading with lock icon
   - Email + Password fields
   - "Login" button
   - No "Register" link (admin accounts created manually in Supabase)
   - No "Forgot Password" (admin resets via Supabase dashboard)
   - Error handling: invalid credentials toast
   - On success: redirect to /admin/dashboard
   - noindex meta tag

2. ADMIN LAYOUT — /app/(admin)/layout.tsx:
   
   SIDEBAR (fixed left, 260px width, dark navy #0F1A2E):
   - Top: "GalaPo" logo + "Admin Panel" subtitle
   - Navigation groups with collapsible sections:
   
     📊 OVERVIEW
     ├── Dashboard
     
     📋 CONTENT MANAGEMENT
     ├── Listings (with pending count badge)
     ├── Categories
     ├── Barangays/Areas
     ├── Deals & Offers
     ├── Events
     ├── Blog Posts
     ├── Static Pages
     
     💰 MONETIZATION
     ├── Subscriptions
     ├── Payments (with pending count badge)
     ├── Invoices
     ├── Ad Management
     ├── Top Search Placements
     
     👥 USERS
     ├── Business Owners
     ├── Claim Requests (with pending count badge)
     
     ⚙️ SYSTEM
     ├── Site Settings
     ├── Notifications
     ├── Annual Checks
     ├── CSV Import/Export
     ├── Analytics
   
   - Bottom of sidebar:
     - "View Public Site" (opens in new tab)
     - Admin profile name
     - Logout button
   
   - Sidebar collapsible to icons-only on desktop (toggle button)
   - Mobile: hidden by default, slide-out on hamburger tap
   
   TOP BAR:
   - Hamburger toggle (for sidebar)
   - Breadcrumb trail
   - Right side: notification bell (with unread count), admin name dropdown
   - Dropdown: Profile, Logout
   
   DESIGN:
   - Sidebar: very dark navy #0F1A2E, white/gray text, coral orange active item + left border accent
   - Content area: light gray #F5F7FA background
   - Cards: white with subtle shadow
   - Professional data-dense dashboard aesthetic

3. ADMIN DASHBOARD — /app/(admin)/admin/dashboard/page.tsx:
   
   SUMMARY STATS ROW (6 cards, 3 per row on desktop, 2 tablet, 1 mobile):
   - Total Active Listings (with ↑↓ vs last month)
   - Pending Approvals (orange if > 0, clickable → goes to pending listings)
   - Pending Payments (orange if > 0, clickable → goes to pending payments)
   - Pending Claims (orange if > 0, clickable → goes to claims)
   - Active Subscriptions (total revenue this month)
   - Total Page Views (this month, all listings combined)
   
   QUICK ACTIONS ROW:
   - "Review Pending Listings" button (shows count)
   - "Verify Payments" button (shows count)
   - "Process Claims" button (shows count)
   - "Create Blog Post" button
   - "Manage Ads" button
   
   RECENT ACTIVITY FEED (left 60%):
   - Timeline of all platform activity:
     - New listing submitted by [business owner] for [listing name]
     - Payment proof uploaded by [business owner] for [plan]
     - Claim request for [listing] by [user]
     - Listing [name] approved by admin
     - Annual check triggered for [listing]
     - Business owner [name] registered
   - Show last 20 activities
   - Each with timestamp, icon, action link
   - "View All" link
   
   PENDING ITEMS (right 40%):
   - Tabbed sections:
     - Tab: Pending Listings (name, owner, date submitted, "Review" button)
     - Tab: Pending Payments (owner, amount, plan, date, "Verify" button)
     - Tab: Pending Claims (listing, claimant, date, "Review" button)
   - Show 5 per tab
   - "View All" link on each tab
   
   REVENUE OVERVIEW:
   - Simple bar chart or stat cards:
     - This month's revenue (from verified payments)
     - Last month's revenue
     - Total all-time revenue
     - Revenue breakdown: Subscriptions vs Ad Placements vs Top Search vs Reactivation
   - (Use simple CSS bar chart or a lightweight chart library — recharts)
   
   TOP PERFORMING LISTINGS:
   - Table of top 10 listings by page views this month
   - Columns: Rank, Listing Name, Category, Views, Clicks, Plan
   
   ANNUAL CHECKS STATUS:
   - "Annual Checks" mini card:
     - X listings due for annual check this week
     - X listings with no response (past deadline)
     - "Manage" button → goes to annual checks page

4. CREATE COMPONENTS:
   - /components/admin/layout/AdminSidebar.tsx
   - /components/admin/layout/AdminTopBar.tsx
   - /components/admin/layout/AdminMobileNav.tsx
   - /components/admin/layout/SidebarNavGroup.tsx — collapsible nav section
   - /components/admin/layout/SidebarNavItem.tsx — single nav item with badge
   - /components/admin/dashboard/AdminStatsCard.tsx
   - /components/admin/dashboard/QuickActions.tsx
   - /components/admin/dashboard/ActivityFeed.tsx
   - /components/admin/dashboard/PendingItems.tsx
   - /components/admin/dashboard/RevenueOverview.tsx
   - /components/admin/dashboard/TopListings.tsx
   - /components/admin/dashboard/AnnualChecksStatus.tsx
   - /components/admin/shared/DataTable.tsx — reusable data table with:
     - Sortable columns (click header to sort)
     - Search/filter input
     - Row selection (checkbox)
     - Bulk actions dropdown
     - Pagination (configurable items per page: 10, 20, 50)
     - Column visibility toggle
     - Export to CSV button
     - Loading skeleton
     - Empty state
     - Responsive (horizontal scroll on mobile)
   - /components/admin/shared/StatusBadge.tsx — colored badge for statuses
   - /components/admin/shared/ConfirmDialog.tsx — reusable confirmation modal
   - /components/admin/shared/AdminSearchInput.tsx — search input with debounce
   - /components/admin/shared/DateRangePicker.tsx — date range filter
   - /components/admin/shared/AdminPageHeader.tsx — page title + breadcrumb + actions

5. ADMIN AUTH GUARD:
   - Create /components/admin/auth/AdminAuthGuard.tsx:
     - Checks Supabase session
     - Checks profile role === 'super_admin'
     - If not admin → redirect to /admin/login
     - Loading state while checking

6. INSTALL recharts (lightweight chart library) for revenue chart.

Output ALL files with complete code. The DataTable component is critical — it will be reused across ALL admin pages.