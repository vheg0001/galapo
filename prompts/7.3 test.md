Continuing the GalaPo project. Create tests for the authentication system.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/auth/RegisterForm.test.tsx:
   - Renders all form fields (name, email, phone, password, confirm)
   - Email validation: shows error for invalid format
   - Phone validation: shows error for non-PH format
   - Password strength indicator:
     - "abc" → weak (red)
     - "abc12345" → medium (yellow)
     - "Abc12345!" → strong (green)
   - Confirm password mismatch shows error
   - Terms checkbox must be checked to submit
   - Submit button disabled when form invalid
   - Submit button shows loading spinner during submission
   - Successful registration redirects to /business/listings/new
   - Duplicate email shows error toast
   - Network error shows error toast

2. /tests/unit/components/auth/LoginForm.test.tsx:
   - Renders email and password fields
   - Show/hide password toggle works
   - Empty fields show validation errors
   - Successful login redirects to /business/dashboard
   - Invalid credentials show error toast
   - "Forgot Password?" link navigates correctly
   - "Register" link navigates correctly
   - Loading spinner during submission

3. /tests/unit/components/auth/ForgotPasswordForm.test.tsx:
   - Renders email field and submit button
   - Invalid email shows error
   - Submit shows success message regardless (security)
   - "Back to Login" link works

4. /tests/unit/components/auth/PasswordStrengthIndicator.test.tsx:
   - Empty → hidden
   - "abc" → weak, red bar (33%)
   - "abc12345" → medium, yellow bar (66%)
   - "Abc12345!@" → strong, green bar (100%)
   - Shows requirements list: min 8 chars, uppercase, number, special char

5. /tests/unit/components/business/Sidebar.test.tsx:
   - Renders all navigation items with correct icons
   - Active route is highlighted
   - Notification badge shows unread count
   - "View Public Site" opens in new tab
   - Logout button calls logout function
   - Mobile: sidebar is hidden by default, opens on hamburger click

6. /tests/unit/components/auth/AuthGuard.test.tsx:
   - Authenticated user → renders children
   - Unauthenticated → redirects to /login
   - Wrong role → redirects to /login
   - Loading → shows spinner

7. /tests/unit/store/authStore.test.ts:
   - login() sets user, profile, session
   - logout() clears all auth state
   - register() sets user and redirects
   - isAuthenticated returns true when session exists
   - isAuthenticated returns false when no session

8. /tests/integration/api/auth.test.ts:
   - POST /api/auth/register with valid data → 201, returns user
   - POST /api/auth/register with duplicate email → 409
   - POST /api/auth/register with invalid email → 400
   - POST /api/auth/register with weak password → 400
   - POST /api/auth/register with missing fields → 400
   - POST /api/auth/login with valid credentials → 200, returns session
   - POST /api/auth/login with wrong password → 401
   - POST /api/auth/login with non-existent email → 401
   - POST /api/auth/logout → 200, session cleared
   - GET /api/auth/session with valid session → returns user
   - GET /api/auth/session without session → returns null
   - POST /api/auth/forgot-password → always 200
   - GET /api/notifications → returns user's notifications
   - PATCH /api/notifications/[id]/read → marks as read
   - PATCH /api/notifications/read-all → marks all as read

9. /tests/unit/lib/auth-helpers.test.ts:
   - validateEmail: valid emails pass, invalid fail
   - validatePhone: PH formats pass (09XX, +63), international fail
   - validatePassword: returns correct strength and messages
   - Test edge cases: empty strings, null, undefined

Output all test files with complete code.