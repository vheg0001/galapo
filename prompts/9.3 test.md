Continuing the GalaPo project. Create tests for the listing management system.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/business/listings/ListingWizard.test.tsx:
   - Renders step 1 (category selection) by default
   - Step progress bar shows correct active step
   - Cannot advance without selecting category
   - Selecting category enables "Next" button
   - Navigation: next/back between steps
   - Cannot skip steps (step 3 not accessible before step 2)
   - Step indicator shows completed steps with checkmark

2. /tests/unit/components/business/listings/CategorySelector.test.tsx:
   - Renders all parent categories as selectable cards
   - Clicking parent category highlights it
   - Subcategories appear after parent selection
   - Selecting subcategory updates form data
   - "Skip" option available for subcategory
   - Changing parent resets subcategory selection

3. /tests/unit/components/business/listings/BasicInfoForm.test.tsx:
   - All fields render correctly
   - Business name: required validation, max 100 chars
   - Short description: required, max 160, char counter visible
   - Phone: PH format validation
   - Website: auto-prefix https://
   - Social media: domain validation (facebook.com, etc.)
   - Tag input: add tag on Enter, remove on X, max 10 tags
   - Payment methods: multi-select checkboxes

4. /tests/unit/components/business/listings/LocationForm.test.tsx:
   - Address text input required
   - Barangay dropdown loads and grouped by area
   - Map renders with Olongapo center
   - Click on map updates lat/lng
   - Marker is draggable
   - "Use my location" button works
   - Operating hours: 7 rows with time pickers
   - "Closed" toggle disables time pickers for that day
   - "Copy Monday to all" fills all days
   - Validation: close > open time

5. /tests/unit/components/business/listings/DynamicFieldsForm.test.tsx:
   - Renders correct fields for selected category
   - TEXT field: renders text input
   - CURRENCY field: renders with ₱ prefix
   - BOOLEAN field: renders toggle switch
   - SELECT field: renders dropdown with options
   - MULTI_SELECT field: renders checkboxes
   - MENU_ITEMS field: renders menu builder
     - Can add item
     - Can remove item
     - Item has name, description, price, photo fields
     - Can reorder items
   - Required fields show asterisk
   - Help text displayed below fields
   - No fields → "No additional details" message

6. /tests/unit/components/business/listings/PhotoUploader.test.tsx:
   - Drag zone renders
   - File selection triggers upload preview
   - Multiple files supported
   - Max 10 photos enforced
   - Invalid file type rejected
   - Oversized file rejected (> 5MB)
   - Photos can be reordered by drag
   - First photo marked as primary
   - Delete button removes photo
   - Sort order updates on reorder

7. /tests/unit/components/business/listings/ReviewStep.test.tsx:
   - Displays all entered data organized by section
   - Category and subcategory shown
   - Business info displayed
   - Map pin visible
   - Operating hours table
   - Dynamic fields with labels and values
   - Photos thumbnails displayed
   - "Edit" links for each section navigate to correct step
   - Terms checkbox required before submit
   - "Submit for Review" button calls submit function
   - "Save as Draft" saves without full validation

8. /tests/unit/components/business/claim/ClaimForm.test.tsx:
   - Shows listing info (read-only)
   - File upload for proof document
   - Accepts PDF, JPG, PNG (max 10MB)
   - Phone field pre-filled from profile
   - Notes textarea optional
   - Submit creates claim request
   - Already claimed → shows "already claimed" message
   - Pending claim → shows "under review" message

9. /tests/integration/api/listing-crud.test.ts:
   - POST /api/business/listings with valid data → 201
   - POST generates unique slug
   - POST with duplicate name → slug gets -2 suffix
   - POST creates listing with status 'pending'
   - POST with missing required fields → 400 with field errors
   - POST with invalid coordinates → 400
   - POST creates notification for admin
   - POST dynamic fields stored in listing_field_values
   - GET /api/business/listings/[id] returns full listing with relations
   - GET /api/business/listings/[id] for other user's listing → 403
   - PUT /api/business/listings/[id] updates listing
   - PUT with critical field change → status back to 'pending'
   - PUT with non-critical change → status stays 'approved'
   - DELETE /api/business/listings/[id] deactivates (is_active = false)
   - POST /api/business/listings/[id]/images uploads and stores
   - POST /api/business/listings/[id]/images over 10 → 400
   - PUT /api/business/listings/[id]/images reorders
   - DELETE /api/business/listings/[id]/images/[imageId] removes
   - GET /api/category-fields/[categoryId] returns fields
   - POST /api/claim with valid data → 201
   - POST /api/claim for already-claimed listing → 409
   - POST /api/claim duplicate claim → 409
   - GET /api/claim/[listingId]/status returns correct status

10. /tests/unit/lib/listing-helpers.test.ts:
    - generateUniqueSlug creates correct slug
    - generateUniqueSlug handles duplicates
    - validateListingData catches all required field errors
    - validateListingData validates dynamic field requirements
    - detectCriticalChanges returns true for name/category/address changes
    - detectCriticalChanges returns false for description/photo changes
    - validateCoordinates passes for Olongapo bounds
    - validateCoordinates fails for Manila coordinates
    - formatOperatingHours validates and formats correctly

11. /tests/unit/store/listingFormStore.test.ts:
    - Initial state: step 1, empty form
    - setStep updates currentStep
    - nextStep increments (with validation)
    - prevStep decrements (min 1)
    - updateFormData updates specific fields
    - validateCurrentStep returns false for incomplete data
    - validateCurrentStep returns true for complete data
    - isDirty tracks changes
    - resetForm clears everything

Output all test files with complete code.