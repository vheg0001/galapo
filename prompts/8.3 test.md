Continuing the GalaPo project. Create tests for the business owner dashboard.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/business/dashboard/StatsCard.test.tsx:
   - Renders icon, label, value correctly
   - Positive change shows green with up arrow
   - Negative change shows red with down arrow
   - Zero change shows neutral

2. /tests/unit/components/business/dashboard/ListingsTable.test.tsx:
   - Renders listing rows with correct data
   - Status badges show correct colors (approved=green, pending=yellow, rejected=red)
   - Plan badges show correct labels
   - "Edit" button navigates to edit page
   - Business name links to public listing page
   - Empty state shows "Add Your First Listing" CTA
   - Views count displays correctly

3. /tests/unit/components/business/dashboard/RecentActivity.test.tsx:
   - Renders activity timeline items
   - Each item shows correct icon based on type
   - Timestamps show "X minutes ago" / "X hours ago" format
   - Empty state shows "No recent activity"

4. /tests/unit/components/business/settings/ProfileForm.test.tsx:
   - Renders pre-filled form with current profile data
   - Name field editable
   - Email field read-only (grayed out)
   - Phone field editable with PH format validation
   - "Save Changes" shows loading on submit
   - Success toast on successful save
   - Error toast on failure
   - Validates required fields

5. /tests/unit/components/business/settings/PasswordForm.test.tsx:
   - Renders current, new, confirm password fields
   - Current password required
   - New password shows strength indicator
   - Confirm must match new
   - Submit calls password update API
   - Success clears form and shows toast
   - Wrong current password shows error

6. /tests/unit/components/business/settings/AvatarUpload.test.tsx:
   - Renders current avatar or placeholder
   - Click triggers file input
   - File selection shows preview
   - Upload calls API and updates avatar
   - Invalid file type shows error (only images)
   - File size limit enforced (5MB)

7. /tests/unit/components/business/notifications/NotificationList.test.tsx:
   - Renders notification items
   - Unread items have highlighted background
   - Click marks as read
   - "Mark All as Read" updates all
   - Filter tabs switch between All/Unread
   - Pagination works
   - Empty state shows correctly

8. /tests/integration/api/business-dashboard.test.ts:
   - GET /api/business/dashboard/stats returns correct stats
   - Stats calculate views/clicks for current month
   - Stats calculate change percentage vs last month
   - GET /api/business/listings returns only user's listings
   - GET /api/business/listings returns all statuses (pending, approved, rejected)
   - GET /api/business/activity returns recent activities
   - GET /api/business/profile returns profile
   - PUT /api/business/profile updates name and phone
   - PUT /api/business/profile/password with correct current password → success
   - PUT /api/business/profile/password with wrong current password → 401
   - POST /api/business/profile/avatar uploads and returns URL
   - All endpoints return 401 for unauthenticated requests

9. /tests/unit/store/businessStore.test.ts:
   - fetchDashboardData populates stats
   - fetchListings populates listings array
   - fetchNotifications populates notifications and unreadCount
   - markNotificationRead updates single notification
   - markAllNotificationsRead updates all

Output all test files with complete code.