Continuing the GalaPo project. Create tests for admin content management.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/admin/categories/CategoryTree.test.tsx:
   - Renders parent categories
   - Expand parent → shows subcategories
   - Click category → selects it (right panel updates)
   - Search filters tree
   - Inactive categories shown grayed out
   - Drag and drop reorders
   - Right-click context menu works

2. /tests/unit/components/admin/categories/CategoryDetail.test.tsx:
   - Renders selected category info
   - Fields are editable
   - Slug auto-generates from name
   - Icon picker opens and selects
   - Save updates category via API
   - Delete shows confirmation (blocked if listings exist)
   - Dynamic fields table renders below

3. /tests/unit/components/admin/categories/FieldEditorModal.test.tsx:
   - Renders all field config inputs
   - Field name auto-generates from label (snake_case)
   - Field type dropdown shows all types
   - Selecting "select" or "multi_select" shows options builder
   - Options builder: add, reorder, delete options
   - Selecting "number" shows min/max inputs
   - Preview section renders field correctly
   - Save calls API
   - Edit mode pre-fills all values
   - Required toggle works
   - Active toggle works

4. /tests/unit/components/admin/categories/IconPicker.test.tsx:
   - Renders grid of Lucide icons
   - Search filters icons
   - Click icon selects it
   - Selected icon shown with checkmark
   - Currently selected icon highlighted

5. /tests/unit/components/admin/barangays/BarangayTable.test.tsx:
   - Renders all barangays with correct grouping
   - Active toggle switches inline
   - Edit button opens modal with pre-filled data
   - Delete blocked when listings reference barangay
   - Add button opens empty modal
   - Search filters by name
   - Drag reorders

6. /tests/unit/components/admin/pages/PageEditor.test.tsx:
   - Rich text editor renders
   - Formatting toolbar works (bold, italic, headings, lists)
   - Image upload works
   - Title and slug fields work
   - Meta title/description fields with char counters
   - Published toggle
   - Save calls API
   - Preview opens in new tab

7. /tests/unit/components/admin/settings/GeneralSettings.test.tsx:
   - Renders all general settings fields pre-filled from database
   - Edit fields and save → API called with updates
   - Validation on required fields
   - Success toast on save

8. /tests/unit/components/admin/settings/PricingSettings.test.tsx:
   - All pricing fields render with ₱ prefix
   - Validation: positive numbers only
   - Save updates site_settings
   - Values reflect in business owner payment pages

9. /tests/integration/api/admin-categories.test.ts:
   - Non-admin → 403
   - GET /api/admin/categories returns tree with counts
   - POST creates category with valid slug
   - POST with duplicate slug → 409
   - PUT updates category fields
   - DELETE empty category → success
   - DELETE category with listings → 409 with count
   - Reorder updates sort_order
   - POST /api/admin/category-fields creates field
   - PUT updates field
   - DELETE field with warning about existing values
   - Reorder fields works

10. /tests/integration/api/admin-barangays.test.ts:
    - Non-admin → 403
    - GET returns all with listing counts
    - POST creates with unique slug
    - PUT updates
    - DELETE empty → success
    - DELETE with listings → 409

11. /tests/integration/api/admin-pages.test.ts:
    - Non-admin → 403
    - CRUD operations work
    - Public GET /api/pages/about returns published page
    - Public GET /api/pages/unpublished-page returns 404

12. /tests/integration/api/admin-settings.test.ts:
    - Non-admin → 403
    - GET returns all settings
    - PUT updates specific settings
    - Pricing validation: negative numbers rejected
    - Public /api/settings/public returns safe settings only

Output all test files with complete code.