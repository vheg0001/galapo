Continuing the GalaPo city directory project.

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI
EXISTING: Admin layout, DataTable component, all APIs built.

Build the complete admin listing management system.

REQUIREMENTS:

1. LISTINGS LIST PAGE — /app/(admin)/admin/listings/page.tsx:
   
   PAGE HEADER:
   - "Listings Management" title
   - Breadcrumb: Admin > Listings
   - Action buttons: "Add Listing" (admin can create on behalf), "Import CSV", "Export CSV"
   
   FILTER BAR:
   - Status filter tabs: All | Pending | Approved | Rejected | Draft | Claimed Pending
   - Each tab shows count
   - Pending tab highlighted orange when count > 0
   - Additional filters (collapsible "More Filters" section):
     - Category dropdown
     - Subcategory dropdown (dynamic based on category)
     - Barangay dropdown
     - Plan: All | Free | Featured | Premium
     - Owner: All | Has Owner | Pre-populated (no owner)
     - Active toggle: All | Active | Inactive
     - Date range: created between [from] - [to]
   - Search input: searches business_name, email, phone
   - "Clear Filters" button
   
   DATA TABLE (using reusable DataTable component):
   - Columns:
     - □ (checkbox for bulk select)
     - ID (short UUID, copyable)
     - Business Name (clickable → opens admin detail view)
     - Category / Subcategory
     - Barangay
     - Owner (name or "Pre-populated")
     - Status (colored badge)
     - Plan (badge)
     - Active (green/red dot)
     - Featured (star icon toggle)
     - Views (this month)
     - Created (date)
     - Actions (dropdown menu)
   
   - Actions dropdown per row:
     - View Public Page (opens in new tab)
     - Edit Listing
     - Approve (if pending)
     - Reject (if pending, opens reason modal)
     - Toggle Active / Inactive
     - Toggle Featured
     - Delete (soft delete with confirmation)
   
   - Bulk actions (when rows selected):
     - Approve Selected
     - Reject Selected (with shared reason)
     - Activate Selected
     - Deactivate Selected
     - Delete Selected
   
   - Sortable columns: Business Name, Category, Status, Created, Views
   - Pagination: 20 per page default, configurable
   - Row count: "Showing 1-20 of 347 listings"

2. LISTING DETAIL / REVIEW PAGE — /app/(admin)/admin/listings/[id]/page.tsx:
   
   This is the admin's detailed view for reviewing and managing a single listing.
   
   TOP BAR:
   - Back button → listings list
   - Listing name as heading
   - Status badge (large)
   - Action buttons:
     - "Approve" (green, if pending/claimed_pending)
     - "Reject" (red, if pending)
     - "Edit" (opens edit form)
     - "Toggle Active" switch
     - "Toggle Featured" switch
     - "View Public Page" (new tab, only if approved)
   
   TWO-COLUMN LAYOUT:
   
   LEFT COLUMN (70%):
   
   SECTION: Business Information
   - All listing fields displayed in read-only format
   - Business name, slug, category, subcategory
   - Short description, full description
   - Tags, payment methods
   - Phone, email, website, social links
   - Address, barangay, city
   - Map with pin
   - Operating hours table
   
   SECTION: Dynamic Fields
   - All category-specific field values displayed with labels
   - Menu items shown as table
   - Empty fields marked as "Not provided"
   
   SECTION: Photos & Logo
   - Logo displayed
   - Photo gallery grid (clickable to enlarge)
   
   SECTION: Deals & Events
   - Active deals list
   - Upcoming events list
   
   RIGHT COLUMN (30%):
   
   SECTION: Listing Meta
   - ID (copyable)
   - Slug
   - Created date
   - Last updated
   - Last verified
   - Is Pre-populated (yes/no)
   - Is Active (toggle)
   - Is Featured (toggle)
   - Is Premium (badge)
   
   SECTION: Owner Info
   - If has owner: Name, email, phone, registered date, total listings
   - If pre-populated: "No owner — Pre-populated by admin"
   - If claimed_pending: Show claimant info + proof document (viewable/downloadable) + approve/reject buttons
   
   SECTION: Subscription
   - Current plan (Free/Featured/Premium)
   - Start date, end date
   - Status (active/expired)
   - Payment history link
   
   SECTION: Analytics Summary
   - Total page views (all time)
   - Views this month
   - Phone clicks, email clicks, website clicks, direction clicks
   - Simple mini chart of daily views (last 30 days)
   
   SECTION: Admin Notes (new feature)
   - Textarea for admin to add private notes about this listing
   - Notes history with timestamp
   - Only visible to super admin
   
   REJECTION FLOW:
   - Click "Reject" → modal opens
   - "Reason for Rejection" textarea (required)
   - Pre-filled reason templates dropdown:
     - "Incomplete information — please provide more details"
     - "Duplicate listing — this business already exists"
     - "Inappropriate content"
     - "Unable to verify business existence"
     - "Custom reason..."
   - "Reject Listing" button
   - On reject: status → 'rejected', notification + email sent to owner with reason
   
   APPROVAL FLOW:
   - Click "Approve" → confirmation dialog
   - "Approve and notify the business owner?" 
   - On approve: status → 'approved', notification + email sent to owner
   - Email includes premium listing benefits ad

3. ADMIN LISTING EDIT — /app/(admin)/admin/listings/[id]/edit/page.tsx:
   - Same form as business owner's listing wizard (reuse components)
   - BUT admin has additional fields:
     - Status dropdown (pending, approved, rejected)
     - Is Featured toggle
     - Is Premium toggle
     - Is Active toggle
     - Owner assignment (search and assign a business owner)
     - Admin notes
   - Admin edits do NOT trigger re-approval (admin is the approver)
   - All fields editable regardless of status

4. ADMIN CREATE LISTING — /app/(admin)/admin/listings/new/page.tsx:
   - Same form as listing wizard
   - Additional: owner assignment (optional — can create without owner for pre-population)
   - Status defaults to 'approved' (admin-created listings auto-approved)
   - Is Pre-populated = true if no owner assigned

5. CLAIM REVIEW PAGE — /app/(admin)/admin/claims/page.tsx:
   - DataTable with all claim requests
   - Columns: Listing Name, Claimant Name, Claimant Email, Submitted Date, Status, Actions
   - Filter tabs: Pending | Approved | Rejected
   - Click row → opens claim detail
   
   CLAIM DETAIL:
   - Listing info (name, category, current status)
   - Claimant info (name, email, phone)
   - Proof document viewer (inline for images, download link for PDF)
   - Admin notes textarea
   - "Approve Claim" and "Reject Claim" buttons
   - Rejection requires reason

6. ADMIN NOTES TABLE (provide SQL):
   - admin_notes table:
     - id (UUID)
     - listing_id (FK)
     - admin_id (FK to profiles)
     - note (TEXT)
     - created_at
   - RLS: only super_admin can CRUD

7. CREATE COMPONENTS:
   - /components/admin/listings/ListingsFilterBar.tsx
   - /components/admin/listings/ListingStatusTabs.tsx
   - /components/admin/listings/ListingDetailView.tsx
   - /components/admin/listings/ListingMetaCard.tsx
   - /components/admin/listings/OwnerInfoCard.tsx
   - /components/admin/listings/AnalyticsSummary.tsx
   - /components/admin/listings/AdminNotesSection.tsx
   - /components/admin/listings/RejectionModal.tsx
   - /components/admin/listings/ApprovalDialog.tsx
   - /components/admin/listings/BulkActionsBar.tsx
   - /components/admin/claims/ClaimDetailView.tsx
   - /components/admin/claims/ProofDocumentViewer.tsx

Output ALL files with complete code including admin_notes SQL.