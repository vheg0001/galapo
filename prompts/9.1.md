Continuing the GalaPo city directory project.

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI, Leaflet
EXISTING: Auth, business layout, dashboard all built.

Build the complete listing management system for business owners.

REQUIREMENTS:

1. CREATE LISTING PAGE — /app/(business)/business/listings/new/page.tsx:
   
   MULTI-STEP FORM (wizard):
   
   PROGRESS BAR at top: Step 1 → Step 2 → Step 3 → Step 4 → Review
   
   STEP 1 — Category Selection:
   - "What type of business do you have?"
   - Grid of parent category cards (same as public categories but selectable)
   - Click parent → show subcategories below:
     - "Select a more specific category (optional but recommended)"
     - Grid/list of subcategories
     - "Skip — just use [Parent Category]" option
   - Selected category highlighted with checkmark
   - "Next" button (disabled until category selected)
   - Changing category resets dynamic fields in later steps
   
   STEP 2 — Basic Information:
   - Business Name (required, max 100 chars)
   - Short Description (required, max 160 chars, char counter, hint: "This appears in search results and SEO")
   - Full Description (textarea, rich text editor with basic formatting: bold, italic, bullet list, links)
   - Tags (tag input: type tag → press Enter to add, click X to remove, max 10)
   - Phone Number (required, PH format)
   - Secondary Phone (optional)
   - Email (optional)
   - Website URL (optional, auto-prefix https:// if missing)
   - Social Media:
     - Facebook URL (optional, validate facebook.com domain)
     - Instagram URL (optional, validate instagram.com)
     - TikTok URL (optional, validate tiktok.com)
   - Payment Methods (multi-select checkboxes): Cash, GCash, Maya, Credit Card, Debit Card, Bank Transfer, PayPal, Other
   
   STEP 3 — Location & Hours:
   - Address (required, street address text input)
   - Barangay (required, dropdown select from active barangays, grouped by area)
   - Map Pin:
     - Leaflet map showing Olongapo
     - "Click on the map to set your exact location" instruction
     - Draggable marker
     - Or: "Use my current location" button (geolocation)
     - Lat/Lng fields (auto-filled from map, also manually editable)
     - Default center: Olongapo City center
   - Operating Hours:
     - 7-day table: Day | Open Time | Close Time | Closed toggle
     - Time pickers for open/close
     - "Closed" toggle per day (disables time pickers)
     - "Copy Monday hours to all days" button (quick fill)
     - "Open 24 Hours" checkbox per day
     - Validation: close time must be after open time (unless 24hr)
   
   STEP 4 — Category-Specific Details (Dynamic Fields):
   - Heading: "[Category] Details"
   - Render all category_fields for the selected category/subcategory
   - Each field renders based on field_type:
     - TEXT: text input with placeholder
     - TEXTAREA: multi-line textarea
     - NUMBER: number input with optional min/max
     - CURRENCY: number input with ₱ prefix
     - BOOLEAN: toggle switch with label
     - SELECT: dropdown with options from field.options
     - MULTI_SELECT: checkbox group or multi-select from field.options
     - URL: URL input
     - PHONE: phone input
     - EMAIL: email input
     - IMAGE_GALLERY: multiple image upload (up to 5 per field)
     - MENU_ITEMS: dynamic list builder:
       - Each item: Name (text), Description (text), Price (currency), Photo (image upload)
       - "Add Menu Item" button
       - Drag to reorder
       - Delete button per item
       - No limit but recommend max 50
     - TIME_RANGE: two time pickers (from - to)
     - JSON: key-value pair builder
   - Required fields marked with red asterisk
   - Help text shown below each field (from field.help_text)
   - If no dynamic fields for this category → show "No additional details needed" and skip
   
   STEP 5 — Photos & Logo:
   - Logo Upload:
     - Square image upload area
     - Preview with crop (1:1 ratio)
     - Max 2MB, jpg/png/webp
     - "Your business logo appears on your listing card"
   - Photo Gallery:
     - Drag-and-drop zone + click to upload
     - Multiple file select
     - Up to 10 photos
     - Each photo: preview thumbnail, delete button, sort drag handle
     - Drag to reorder
     - First photo = primary photo (shown in cards/search)
     - Max 5MB each, jpg/png/webp
     - "Photos help customers learn about your business"
   
   STEP 6 — Review & Submit:
   - Full preview of all entered information
   - Organized by sections matching the steps
   - "Edit" link on each section (goes back to that step)
   - Category-specific fields shown with labels and values
   - Photos displayed as thumbnail grid
   - Map preview with pin
   - Operating hours table
   - Prominent notice:
     - "Your listing will be reviewed by our team and typically approved within 24-48 hours."
     - "You'll receive an email notification once approved."
   - Premium upsell box:
     - "Want more visibility? Upgrade to Featured or Premium!"
     - Brief benefits comparison
     - "You can upgrade anytime from your dashboard"
   - "Submit for Review" button (coral orange, large)
   - "Save as Draft" button (outline, smaller) — saves but doesn't submit
   - Terms checkbox: "I confirm this is my business and all information is accurate"

2. EDIT LISTING PAGE — /app/(business)/business/listings/[id]/edit/page.tsx:
   - Same form as create, but pre-filled with existing data
   - Same multi-step wizard layout
   - Changes: "Submit for Review" → "Save Changes" button
   - If listing is approved and user edits critical fields (name, category, address):
     - Show warning: "Changing these fields will put your listing back into review"
     - Listing status changes to 'pending' on save (re-approval needed)
   - If listing is approved and user edits non-critical fields (description, photos, hours):
     - Save without changing status (no re-approval needed)
   - Critical fields: business_name, category_id, subcategory_id, address, barangay_id
   - Non-critical: description, photos, hours, dynamic fields, social links, tags

3. MY LISTINGS PAGE — /app/(business)/business/listings/page.tsx:
   - "My Listings" heading
   - "Add New Listing" button (top right)
   - If no listings: empty state with illustration + "Add your first business listing" CTA
   - Listings displayed as cards or table rows:
     - Primary image thumbnail
     - Business name
     - Category / Subcategory tags
     - Status badge (Approved/Pending/Rejected)
     - Plan badge (Free/Featured/Premium)
     - Views this month
     - Created date
     - Actions: "Edit", "View Public Page", "Manage Deals", "Manage Events"
   - If rejected: show rejection reason in expandable row
   - If pending: show "Under review" message
   - Sort: newest first
   - Can manage multiple listings

4. CLAIM LISTING PAGE — /app/(public)/claim/[slug]/page.tsx:
   - For unclaimed pre-populated listings
   - Must be logged in (redirect to register if not)
   - Shows listing info: name, address, category (read-only preview)
   - Claim Form:
     - "Prove You Own This Business"
     - Upload field: "Business Permit or DTI Certificate" (PDF, JPG, PNG, max 10MB)
     - Phone number (pre-filled from profile, editable)
     - Additional notes (optional textarea)
     - "Submit Claim Request" button
   - After submission:
     - Listing status → 'claimed_pending'
     - Super admin notified
     - Confirmation page: "Your claim has been submitted. We'll review it within 1-3 business days. You'll receive an email once verified."
   - If listing is already claimed: show "This business has already been claimed"
   - If user already has a pending claim for this listing: show "Your claim is under review"

5. CREATE COMPONENTS:
   - /components/business/listings/ListingWizard.tsx — multi-step form controller
   - /components/business/listings/StepProgress.tsx — step progress bar
   - /components/business/listings/CategorySelector.tsx — category + subcategory grid selection
   - /components/business/listings/BasicInfoForm.tsx — step 2 form fields
   - /components/business/listings/LocationForm.tsx — address, barangay, map pin, hours
   - /components/business/listings/DynamicFieldsForm.tsx — renders dynamic fields based on category
   - /components/business/listings/DynamicFieldRenderer.tsx — renders single dynamic field by type
   - /components/business/listings/MenuItemBuilder.tsx — menu item list builder
   - /components/business/listings/PhotoUploader.tsx — drag-drop multi-photo upload with reorder
   - /components/business/listings/LogoUploader.tsx — single logo upload with crop
   - /components/business/listings/OperatingHoursEditor.tsx — 7-day hours editor
   - /components/business/listings/MapPinSelector.tsx — Leaflet map for picking location
   - /components/business/listings/TagInput.tsx — tag input with chips
   - /components/business/listings/ReviewStep.tsx — preview all data before submit
   - /components/business/listings/ListingCard.tsx — listing card for My Listings page (different from public card)
   - /components/business/listings/RichTextEditor.tsx — basic rich text editor for descriptions
   - /components/business/claim/ClaimForm.tsx — claim business form

6. CREATE ZUSTAND STORE:
   - /store/listingFormStore.ts:
     - currentStep: number (1-6)
     - formData: {
         category_id, subcategory_id,
         business_name, short_description, full_description, tags, phone, phone_secondary, email, website, social_links, payment_methods,
         address, barangay_id, lat, lng, operating_hours,
         dynamic_fields: { field_id: value }[],
         logo: File | null, logo_preview: string,
         photos: { file: File, preview: string, sort_order: number }[]
       }
     - errors: { [field]: string }
     - isDirty: boolean
     - isSubmitting: boolean
     - setStep(step)
     - nextStep()
     - prevStep()
     - updateFormData(partial)
     - setFieldError(field, message)
     - clearErrors()
     - resetForm()
     - validateCurrentStep(): boolean
     - submitListing()
     - saveDraft()

7. UNSAVED CHANGES WARNING:
   - If user tries to navigate away with unsaved changes:
     - Show browser beforeunload confirmation
     - Show in-app confirmation dialog: "You have unsaved changes. Are you sure you want to leave?"

Output ALL files with complete code. Ensure the dynamic field renderer handles all 14 field types correctly.