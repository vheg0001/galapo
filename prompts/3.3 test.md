Continuing the GalaPo project. Create tests for the homepage components and API routes.

TECH: Vitest, React Testing Library, MSW v2

REQUIREMENTS:

1. /tests/unit/components/shared/Header.test.tsx:
   - Renders logo with correct link to home
   - Renders all navigation links
   - Shows "List Your Business" button
   - Mobile: hamburger menu toggles drawer
   - Active link is highlighted

2. /tests/unit/components/shared/Footer.test.tsx:
   - Renders all column links
   - Renders copyright with current year
   - Social icons have correct links

3. /tests/unit/components/shared/SearchBar.test.tsx:
   - Renders search input, category dropdown, barangay dropdown, search button
   - Submitting form navigates to /search with correct query params
   - Empty search shows validation

4. /tests/unit/components/shared/ListingCard.test.tsx:
   - Renders business name, category, barangay, short description
   - Shows featured badge when is_featured = true
   - Shows premium badge when is_premium = true
   - Phone number is clickable (tel: link)
   - Links to correct listing detail page

5. /tests/unit/components/shared/AdSlot.test.tsx:
   - Renders internal ad when available
   - Falls back to AdSense when no internal ad
   - Renders nothing when neither is available
   - Click tracks ad click

6. /tests/unit/components/shared/SocialShareButtons.test.tsx:
   - Facebook share opens correct URL
   - Twitter share opens correct URL
   - Copy link copies to clipboard

7. /tests/integration/api/homepage.test.ts:
   - Mock Supabase responses with MSW
   - Test GET /api/listings/featured returns correct shape
   - Test GET /api/listings/latest returns correct shape
   - Test GET /api/categories returns nested structure with counts
   - Test GET /api/events/upcoming returns only future events
   - Test GET /api/deals/active returns only active deals
   - Test GET /api/ads?location=homepage_banner returns active ads
   - Test POST /api/ads/click increments click count
   - Test error handling for each route (500, invalid params)

8. /tests/unit/components/public/HomePage.test.tsx:
   - Renders hero section with search bar
   - Renders featured listings section (or hides if empty)
   - Renders categories grid
   - Renders events section (or hides if empty)
   - Renders deals section (or hides if empty)
   - Renders blog preview
   - Renders CTA banner

Output all test files with complete code.