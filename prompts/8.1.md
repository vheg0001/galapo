Continuing the GalaPo city directory project.

TECH: Next.js 16 App Router, TypeScript, Tailwind CSS v4, Shadcn/UI
EXISTING: Auth system, business layout with sidebar all built.

Build the business owner dashboard overview and account settings.

REQUIREMENTS:

1. DASHBOARD OVERVIEW — /app/(business)/business/dashboard/page.tsx:
   
   WELCOME SECTION:
   - "Welcome back, [Full Name]!" heading
   - Current date in Philippine format
   - Quick action buttons row:
     - "Add New Listing" (coral orange, primary)
     - "Create Deal" (outline)
     - "Create Event" (outline)
   
   STATS CARDS (4-column grid, 2 on tablet, 1 on mobile):
   - Card 1: "Total Listings" — count of user's listings, icon: 🏪
   - Card 2: "Total Views" — sum of page_views across all listings (this month), icon: 👁️
   - Card 3: "Total Clicks" — sum of contact clicks (phone+email+website+directions) this month, icon: 📱
   - Card 4: "Active Deals" — count of active deals, icon: 🏷️
   - Each card: icon, label, large number, small "vs last month" percentage change (green up / red down)
   
   MY LISTINGS SECTION:
   - "My Listings" heading with "View All" link
   - Show first 5 listings in a compact table/list:
     - Business name (clickable → listing detail on public site)
     - Status badge: Approved (green), Pending (yellow), Rejected (red)
     - Plan badge: Free, Featured (orange), Premium (gold)
     - Views this month
     - "Edit" button
   - If no listings: empty state with "Add Your First Listing" CTA button
   
   RECENT ACTIVITY:
   - "Recent Activity" heading
   - Timeline list of recent events:
     - "Your listing [name] was approved" (with timestamp)
     - "You created a new deal for [name]"
     - "Your premium subscription expires in 7 days"
     - "Payment confirmed for [listing name]"
   - Show last 10 activities
   - If no activities: "No recent activity"
   
   NOTIFICATIONS PREVIEW:
   - "Recent Notifications" heading with "View All" link
   - Show latest 5 unread notifications
   - Each: icon, title, message preview, time ago
   - Click → goes to notifications page
   
   SUBSCRIPTION STATUS (if any active subscription):
   - Card showing:
     - Current plan (Featured/Premium)
     - Listing name it's for
     - Expires: [date]
     - "Renew" button
     - If no subscription: "Upgrade to Featured/Premium" CTA with benefits teaser

2. ACCOUNT SETTINGS — /app/(business)/business/settings/page.tsx:
   
   TAB LAYOUT:
   
   TAB 1 — Profile:
   - Full Name (editable)
   - Email (read-only, grayed out)
   - Phone (editable)
   - Avatar upload (circular image, click to upload, crop modal)
   - "Save Changes" button
   - Success/error toast on save
   
   TAB 2 — Password:
   - Current Password (required)
   - New Password (with strength indicator)
   - Confirm New Password
   - "Update Password" button
   - Success toast on update
   
   TAB 3 — Notifications Preferences:
   - Toggle switches for each notification type:
     - Email notifications for listing approval/rejection
     - Email notifications for subscription expiry
     - Email notifications for annual check reminders
     - Email notifications for payment confirmations
   - "Save Preferences" button
   - (Store in profile or a separate notification_preferences JSONB column)

3. NOTIFICATIONS PAGE — /app/(business)/business/notifications/page.tsx:
   
   - "Notifications" heading with unread count
   - "Mark All as Read" button
   - Filter tabs: All | Unread
   - Notification list:
     - Each notification: icon (based on type), title, message, time ago, read/unread indicator
     - Unread: slightly highlighted background
     - Click → marks as read + navigates to relevant page (e.g., listing page, billing page)
   - Pagination (20 per page)
   - Empty state: "No notifications yet"

4. CREATE COMPONENTS:
   - /components/business/dashboard/StatsCard.tsx — single stat card with icon, value, change
   - /components/business/dashboard/ListingsTable.tsx — compact listings list
   - /components/business/dashboard/RecentActivity.tsx — activity timeline
   - /components/business/dashboard/NotificationsPreview.tsx — notification preview
   - /components/business/dashboard/SubscriptionStatus.tsx — current plan card
   - /components/business/dashboard/QuickActions.tsx — action button row
   - /components/business/settings/ProfileForm.tsx — edit profile form
   - /components/business/settings/PasswordForm.tsx — change password form
   - /components/business/settings/NotificationPreferences.tsx — toggle switches
   - /components/business/settings/AvatarUpload.tsx — avatar upload with crop
   - /components/business/notifications/NotificationList.tsx — notification items
   - /components/business/notifications/NotificationItem.tsx — single notification

5. CREATE ZUSTAND STORE:
   - /store/businessStore.ts:
     - listings: Listing[]
     - stats: { totalListings, totalViews, totalClicks, activeDeals, viewsChange, clicksChange }
     - notifications: Notification[]
     - unreadCount: number
     - isLoading: boolean
     - fetchDashboardData()
     - fetchListings()
     - fetchNotifications()
     - markNotificationRead(id)
     - markAllNotificationsRead()

Output ALL files with complete code.